apiVersion: v1
kind: Service
metadata:
  name: {{ include "nvd-mcp.fullname" . }}
  labels:
    {{- include "nvd-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nvd-mcp.selectorLabels" . | nindent 4 }}
